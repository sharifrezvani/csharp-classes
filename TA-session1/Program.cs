using System;

namespace TASession1
{
    internal class Program
    {
        static void Main(string[] args)
        {
            int rows = 5;
            
            for (int i = 1; i <= rows; i++)
            {
                // First Exercise -------------------------------------------------------------
                
                // string result = "";

                // result += String.Concat(Enumerable.Repeat(" ", rows - i));

                // for (int a = 1; a <= i; a++)
                // {
                //     result += a;
                // }

                // for (int j = i - 1; j > 0; j--)
                // {
                //     result += j;
                // }

                // result += String.Concat(Enumerable.Repeat(" ", rows - i));
                
                // Console.WriteLine(result);

                // Second Exercise ------------------------------------------------------------



                string input = "";
                int sum = 0;
                
                

            }
        }
    }
}